[pytest]
testpaths = tests
norecursedirs = benchmarks .venv
pythonpath = .
markers =
    integration: requires embeddings + Chroma (slower)
    live: network downloads from OEM / GitHub
addopts = -ra --ignore=tests/test_workflow.py
